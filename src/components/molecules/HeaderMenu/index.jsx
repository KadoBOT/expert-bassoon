import React, {PropTypes} from 'react'

const HeaderMenu = () => {
  return(
    <div>
      Header Menu!
    </div>
  )
}

HeaderMenu.propTypes = {
  props: PropTypes.type
}

export default HeaderMenu
