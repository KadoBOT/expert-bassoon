import React, {PropTypes} from 'react'

const FooterMenu = () => {
  return(
    <div>
      Footer Menu!
    </div>
  )
}

FooterMenu.propTypes = {
  props: PropTypes.type
}

export default FooterMenu
