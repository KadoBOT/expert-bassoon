import React, {PropTypes} from 'react'
import './header.css'

const Header = () => {
  return(
    <div className="header">
      Header
    </div>
  )
}

Header.propTypes = {
  props: PropTypes.type
}

export default Header
