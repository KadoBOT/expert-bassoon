import React, {PropTypes} from 'react'
import './slider.css'

const Slider = () => {
  return(
    <div className="slider">
      Slider!
    </div>
  )
}

Slider.propTypes = {
  props: PropTypes.type
}

export default Slider
